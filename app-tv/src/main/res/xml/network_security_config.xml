<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base config for all connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- Domain specific configurations -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Allow cleartext traffic for development servers -->
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        
        <!-- Add your API domains here if needed -->
        <!-- <domain includeSubdomains="true">api.vega.com</domain> -->
    </domain-config>
    
    <!-- Debug overrides - only applied in debug builds -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config> 